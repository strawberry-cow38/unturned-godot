# GENERATED by tools/gen_vehicle_glass.py -- pane 'windshield'. Do not hand-edit.
# One file per pane so each breaks independently (Vehicle.GlassPaneLabels).
g windshield
# SIMPLIFIED by tools/simplify_glass_panes.py -- outline only (see its docstring).
# RE-RAKED by tools/add_sedan_dash.py: laid into the A-pillar plane (master: match the pillar slope).
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
# RE-RAKED by tools/add_sedan_dash.py to the A-pillar's OUTSIDE slant (beltline -> roof leading edge),
# which is steeper than the aperture's own front edge -- see the tool for why those differ.
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
# RE-RAKED by tools/add_sedan_dash.py to the A-PILLAR'S REAR EDGE -- the diagonal the pillar reads as,
# corroborated by sedan_glass_r_front, whose leading edge is cut to that same pillar at slope 0.550.
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
# RE-RAKED by tools/add_sedan_dash.py onto the A-PILLAR'S MIDLINE -- halfway between its front and
# rear faces, so the pane is bonded INTO the frame rather than lying against the back of it.
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
# RE-RAKED by tools/add_sedan_dash.py: the A-PILLAR'S REAR-EDGE ANGLE, slid forward to pass through
# the middle of the pillar. Angle and position are separate -- taking the angle off the midline
# instead flattens the screen, because a wedge's midline is shallower than either of its faces.
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
# RE-RAKED by tools/add_sedan_dash.py: the A-PILLAR'S REAR-EDGE ANGLE, slid forward to pass through
# the middle of the pillar. Angle and position are separate -- taking the angle off the midline
# instead flattens the screen, because a wedge's midline is shallower than either of its faces.
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
# RE-RAKED by tools/add_sedan_dash.py: angle = the chord of sedan_glass_r_front's leading edge (that
# door pane is cut to this same A-pillar); position = a quarter of the pillar's depth back from its
# front face. Angle and position are separate -- re-deriving one while moving the other flattens it.
# Regenerating this pane with gen_vehicle_glass.py will undo that -- the rake lives HERE, not there.
v -1.014036 1.125000 -1.310505
v 1.015964 1.125000 -1.310505
v 1.015964 1.875000 -0.841755
v -1.014036 1.875000 -0.841755
f 1 2 3
f 1 3 4
